import 'package:flutter/material.dart';

class RainfallCurrentDataWidget extends StatelessWidget {
  const RainfallCurrentDataWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
